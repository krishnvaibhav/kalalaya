import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#D9A552",
    },
    secondary: {
      main: "#704241",
    },
    dark: {
      main: "#1a1a1a",
    },
    light: {
      main: "#FFF",
    },
  },
});
